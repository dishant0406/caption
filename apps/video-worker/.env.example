# Node Environment
NODE_ENV=development

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=your-azure-storage-connection-string
AZURE_STORAGE_CONTAINER_NAME=caption-videos

# ===========================================
# Transcription Provider Configuration
# ===========================================
# Supported providers: azure-openai, azure-gpt4o, openai, deepgram, assemblyai
# - azure-openai: Uses Azure OpenAI Whisper model (recommended for accuracy)
# - azure-gpt4o: Uses Azure GPT-4o transcription model (newer, supports diarization)
TRANSCRIPTION_PROVIDER=azure-openai

# Azure OpenAI Whisper Configuration
# Used when TRANSCRIPTION_PROVIDER=azure-openai
# Get these from your Azure OpenAI resource in Azure Portal
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_WHISPER_DEPLOYMENT=whisper
AZURE_OPENAI_API_VERSION=2024-02-01

# Azure GPT-4o Transcription Configuration
# Used when TRANSCRIPTION_PROVIDER=azure-gpt4o
# Uses Bearer token auth instead of api-key header
# If not specified, falls back to AZURE_OPENAI_* values
AZURE_GPT4O_ENDPOINT=https://your-resource.openai.azure.com
AZURE_GPT4O_API_KEY=your-azure-gpt4o-api-key
AZURE_GPT4O_DEPLOYMENT=gpt-4o-transcribe-diarize
AZURE_GPT4O_API_VERSION=2025-03-01-preview

# OpenAI Configuration (Fallback/Alternative)
# Used when TRANSCRIPTION_PROVIDER=openai or as fallback
OPENAI_API_KEY=your-openai-api-key

# ===========================================
# FFmpeg Configuration
# ===========================================
# Optional - uses system PATH if not specified
# FFMPEG_PATH=/usr/local/bin/ffmpeg
# FFPROBE_PATH=/usr/local/bin/ffprobe

# ===========================================
# Worker Configuration
# ===========================================
WORKER_CONCURRENCY=2
MAX_RETRY_ATTEMPTS=3

# Temp Directory for video processing
TEMP_DIR=/tmp/caption-worker

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=info
